---
description: Feature Spec Kit ativa
alwaysApply: true
---

# Feature ativa

1. Ler `.specify/feature.json` → `feature_directory`.
2. Usar `spec.md`, `plan.md`, `tasks.md` dessa pasta.
3. Marcar `[X]` em `tasks.md` ao concluir.
4. Não expandir escopo além de `spec.md`.
